# ================================
# TimeTravel Agency — Frontend ENV
# ================================
# Laisser vide en développement (le proxy Vite gère /api)
# En production, pointer vers l'URL publique du backend
VITE_API_URL=
# Exemple prod : VITE_API_URL=https://timetravel-api.up.railway.app
